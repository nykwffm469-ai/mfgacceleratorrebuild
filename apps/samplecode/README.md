# Sample Code (2026)

This folder keeps the original `apps/samplecode` location but now hosts a modern modular stack:

- `web`: Next.js front-end for supplier operations and copilot interactions
- `api`: Fastify back-end for domain workflows and AI orchestration
- `agents`: reusable agent routines for supplier-risk and qualification workflows
- `packages/domain`: shared typed contracts and validation schemas
