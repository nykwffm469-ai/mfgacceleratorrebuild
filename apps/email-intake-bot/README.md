# email-intake-bot

Shared-mailbox automation console - Outlook-style 3-pane mailbox with an RPA bot that watches the inbox, auto-classifies incoming messages, extracts structured data, and routes to downstream systems.
