# inventory-cyclecount-console

Cycle count task waves, handheld count uploads, variance approvals, and recount operations.

Run:

```bash
corepack pnpm --filter @legacy/inventory-cyclecount-console dev
```
