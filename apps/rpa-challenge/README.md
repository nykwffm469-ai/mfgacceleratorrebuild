# rpa-challenge

RPA challenge app with stable field set that scrambles order after every submit for resilience and self-healing automation tests.

Run:

```bash
corepack pnpm --filter @legacy/rpa-challenge dev
```
