# Schema Overview

The canonical schema is maintained in:

- `CDS.solutions/Dynamics365ManufacturingAccelerator/domain-model.json`

This schema drives API contracts and downstream persistence model generation.
