# Contributing

## Development

1. Install dependencies: `pnpm install`
2. Start local services: `pnpm dev`
3. Run checks before PR: `pnpm lint && pnpm typecheck && pnpm test`

## Pull requests

- Keep changes scoped and documented.
- Add tests for behavior changes.
- Update `documentation/` when workflows or architecture change.
