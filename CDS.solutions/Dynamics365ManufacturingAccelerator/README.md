# Dynamics365ManufacturingAccelerator (Modern Blueprint)

This folder preserves the legacy solution location but now contains portable, source-controlled accelerator assets.

## Contents

- `solution.manifest.json`: high-level solution metadata
- `domain-model.json`: canonical supplier lifecycle entities and relationships

Use these assets as the source of truth before generating Dataverse artifacts.
